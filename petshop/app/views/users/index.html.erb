<h1>Listing users</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Dni</th>
    <th>Telephone</th>
    <th>Rest</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.dni %></td>
    <td><%= user.telephone %></td>
    <td><%= user.rest %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>

<p></br><%= link_to 'Listing Pets', pets_path %></p>

<p><%= link_to 'Listing Pays', pays_path %></p>

<p><a href="index.html">Home</a></p>